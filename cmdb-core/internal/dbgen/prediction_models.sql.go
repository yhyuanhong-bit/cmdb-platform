// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: prediction_models.sql

package dbgen

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const createModel = `-- name: CreateModel :one
INSERT INTO prediction_models (
    name, type, provider, config, enabled
) VALUES (
    $1, $2, $3, $4, $5
) RETURNING id, name, type, provider, config, enabled, created_at
`

type CreateModelParams struct {
	Name     string          `json:"name"`
	Type     string          `json:"type"`
	Provider string          `json:"provider"`
	Config   json.RawMessage `json:"config"`
	Enabled  bool            `json:"enabled"`
}

func (q *Queries) CreateModel(ctx context.Context, arg CreateModelParams) (PredictionModel, error) {
	row := q.db.QueryRow(ctx, createModel,
		arg.Name,
		arg.Type,
		arg.Provider,
		arg.Config,
		arg.Enabled,
	)
	var i PredictionModel
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Provider,
		&i.Config,
		&i.Enabled,
		&i.CreatedAt,
	)
	return i, err
}

const getModel = `-- name: GetModel :one
SELECT id, name, type, provider, config, enabled, created_at FROM prediction_models WHERE id = $1
`

func (q *Queries) GetModel(ctx context.Context, id uuid.UUID) (PredictionModel, error) {
	row := q.db.QueryRow(ctx, getModel, id)
	var i PredictionModel
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Type,
		&i.Provider,
		&i.Config,
		&i.Enabled,
		&i.CreatedAt,
	)
	return i, err
}

const listAllModels = `-- name: ListAllModels :many
SELECT id, name, type, provider, config, enabled, created_at FROM prediction_models
ORDER BY name
`

func (q *Queries) ListAllModels(ctx context.Context) ([]PredictionModel, error) {
	rows, err := q.db.Query(ctx, listAllModels)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PredictionModel{}
	for rows.Next() {
		var i PredictionModel
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.Provider,
			&i.Config,
			&i.Enabled,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listEnabledModels = `-- name: ListEnabledModels :many
SELECT id, name, type, provider, config, enabled, created_at FROM prediction_models
WHERE enabled = true
ORDER BY name
`

func (q *Queries) ListEnabledModels(ctx context.Context) ([]PredictionModel, error) {
	rows, err := q.db.Query(ctx, listEnabledModels)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PredictionModel{}
	for rows.Next() {
		var i PredictionModel
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Type,
			&i.Provider,
			&i.Config,
			&i.Enabled,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
