# tenantlint baseline — per-file violation counts.
#
# CI (see scripts/tenantlint-check.sh) fails when any file's count
# exceeds its baseline OR when a file not in this list gains its first
# violation.
#
# Rules for contributors:
#   1. DO NOT raise a count here to silence a new finding. Either fix
#      the call to go through database.TenantScoped or add the
#      //tenantlint:allow-direct-pool opt-out comment at the top of
#      the file with a justification in the commit message.
#   2. LOWER counts here whenever you fix violations — that is how the
#      backlog burns down. A fixed file becomes the new ceiling.
#   3. Remove a line entirely once its count reaches 0.
#
# Format: <path-relative-to-cmdb-core> <count>
#
# Status as of 2026-04-22: baseline empty. Original 180-violation
# backlog burned down to zero in commits f74f9be + 23ed580. All
# remaining direct-pool calls either use database.TenantScoped or
# carry a //tenantlint:allow-direct-pool marker with justification.
# Any new entry here is a regression.
