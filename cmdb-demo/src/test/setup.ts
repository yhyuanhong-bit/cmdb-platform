import '@testing-library/jest-dom'
// Initialize i18next once for all tests so components using useTranslation
// resolve real strings instead of throwing or returning keys unpredictably.
import '../i18n'
